server:
  shutdown: graceful
  forward-headers-strategy: framework

spring:
  profiles:
    default: local

  application:
    name: notification-service

  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        default_schema: notification
        jdbc:
          time_zone: UTC

  flyway:
    locations: classpath:db/migration/postgresql

management:
  prometheus:
    metrics:
      export:
        enabled: true

  endpoints:
    web:
      exposure:
        include: health,info,prometheus

  endpoint:
    health:
      probes:
        enabled: true

lifebalance:
  integration:
    internal-secret: ${LIFEBALANCE_INTERNAL_SERVICE_SECRET:}
  security:
    keycloak:
      client-id: ${KEYCLOAK_CLIENT_ID:lifebalance-api}
  notification:
    default-channel: ${NOTIFICATION_DEFAULT_CHANNEL:IN_APP}

info:
  app:
    name: LifeBalance Notification Service
    version: 1.0.0
    description: Notification API

logging:
  level:
    root: INFO
    com.lifebalance.notification: INFO
    org.springframework.security: INFO
    org.springframework.web: INFO

  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] %logger - %msg%n"
